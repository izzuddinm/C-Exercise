#include <stdio.h>
#include <stdbool.h>

int main()
{
    char x = 'X';
    char y = 'X';
    bool isSame = false;
    if (x == y)
    {
        isSame = true;
        printf("%d", isSame);
    }
    else
    {
        printf("%d", isSame);
    }

    return 0;
}
